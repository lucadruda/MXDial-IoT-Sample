{
  "private": true,
  "workspaces": [
    "common",
    "mx"
  ],
  "scripts": {    
    "start": "yarn --cwd mxyarn  start",    
    "build-common": "yarn --cwd common build",
    "build-mx": "yarn --cwd mx build",
    "build":"yarn build-common && yarn build-mx"
 }
}
